# 📋 قائمة المهام - الإصلاحات الواجب إجراؤها فوراً

**الحالة:** جاري العمل عليها  
**آخر تحديث:** 2026-06-13

---

## 🔴 P0 - حرجة (إصلاحات فورية)

### [ ] P0.1 - إصلاح نموذج المستخدم - إضافة الاسم الأول والأخير
**الملفات:** 
- `backend/database/migrations/XXXX_create_users_table.php`
- `backend/app/Models/User.php`
- `backend/app/Http/Controllers/Api/AuthController.php`

**المهام:**
- [ ] إنشاء ملف الهجرة: `php artisan make:migration add_names_to_users_table`
- [ ] إضافة أعمدة `first_name` و `last_name` إلى جدول users
- [ ] إضافة الحقول إلى fillable في نموذج User
- [ ] تعديل AuthController::register() لتخزين الأسماء في User أيضاً
- [ ] تعديل AuthController::getUserData() لإرجاع الأسماء من User
- [ ] اختبار أن البيانات متزامنة بشكل صحيح

**الوقت المقدر:** 1-2 ساعة

---

### [ ] P0.2 - إضافة التحقق من صحة جميع نقاط النهاية
**الملفات:** 
- `backend/app/Http/Requests/` (إنشاء ملفات جديدة)
- جميع المتحكمات

**المهام:**
- [ ] إنشاء `StudentStoreRequest.php` لإنشاء/تعديل الطالب
- [ ] إنشاء `TeacherStoreRequest.php` لإنشاء/تعديل المعلم
- [ ] إنشاء `ModuleStoreRequest.php` للمودول
- [ ] إضافة التحقق في كل متحكم:
  - AdminController::createStudent()
  - AdminController::updateStudent()
  - AdminController::createTeacher()
  - AdminController::updateTeacher()
  - AdminController::createModule()
  - AdminController::updateModule()
  - StudentController::updateProfile()
  - TeacherController::updateProfile()
- [ ] التأكد من إرجاع أخطاء التحقق (422)
- [ ] الاختبار ببيانات غير صحيحة

**الوقت المقدر:** 3-4 ساعات

---

### [ ] P0.3 - إصلاح محاذاة مسارات API
**الملف:** `backend/routes/api.php`

**المهام:**
- [ ] محاذاة صحيحة لمسارات الطالب (الأسطر 57-62)
- [ ] محاذاة صحيحة لمسارات المعلم (الأسطر 109-115)
- [ ] التحقق من أن جميع المسارات يمكن الوصول إليها: `php artisan route:list | grep -E "(student|teacher|admin)"`
- [ ] اختبار كل نقطة نهاية مع Postman/Insomnia
- [ ] التحقق من تطبيق Middleware بشكل صحيح

**الوقت المقدر:** 1 ساعة

---

### [ ] P0.4 - إصلاح Auth Store - التعامل مع جميع الأدوار في fetchUser()
**الملف:** `frontend/src/stores/auth.js:120`

**المهام:**
- [ ] تعديل fetchUser() للكشف عن الدور
- [ ] إذا كان Student: استدعاء studentApi.getProfile()
- [ ] إذا كان Teacher: استدعاء teacherApi.getProfile()
- [ ] إذا كان Admin: استدعاء adminApi.getProfile()
- [ ] إنشاء teacherApi و adminApi إذا لزم الأمر
- [ ] اختبار التحديث لكل دور

**الكود المراد إضافته:**
```javascript
async function fetchUser() {
  if (!token.value) return
  try {
    let data
    if (user.value?.role === 'student') {
      data = await studentApi.getProfile()
    } else if (user.value?.role === 'teacher') {
      data = await teacherApi.getProfile()  // يتم إنشاؤها
    } else if (user.value?.role === 'administration') {
      data = await adminApi.getProfile()    // يتم إنشاؤها
    } else {
      return
    }
    user.value = data.data || data
  } catch (err) {
    // ... معالجة الأخطاء
  }
}
```

**الوقت المقدر:** 1-2 ساعة

---

### [ ] P0.5 - إضافة مقاطع اعتراض خطأ الشبكة (Axios)
**الملف:** `frontend/src/api/axios.js`

**المهام:**
- [ ] إضافة مقطع اعتراض لاستجابات 401 (غير مصرح)
- [ ] إضافة مقطع اعتراض لاستجابات 403 (ممنوع)
- [ ] إضافة مقطع اعتراض لاستجابات 500 (خطأ الخادم)
- [ ] عند 401: تسجيل خروج المستخدم
- [ ] عرض رسالة خطأ واضحة للمستخدم
- [ ] الاختبار برمز منتهية صلاحيته

**هيكل الكود:**
```javascript
apiClient.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      // التعامل مع عدم التفويض
    } else if (error.response?.status === 403) {
      // التعامل مع الممنوع
    } else if (error.response?.status >= 500) {
      // التعامل مع خطأ الخادم
    }
    return Promise.reject(error)
  }
)
```

**الوقت المقدر:** 1-2 ساعة

---

### [ ] P0.6 - إضافة معاملات قاعدة البيانات للعمليات الحرجة
**الملفات:** 
- `backend/app/Http/Controllers/Api/AdminDeliberationController.php`
- `backend/app/Http/Controllers/Api/AdminController.php` (createStudent، إلخ)

**المهام:**
- [ ] لف storeOrUpdate() في AdminDeliberationController مع DB::transaction()
- [ ] لف createStudent() في DB::transaction()
- [ ] لف createTeacher() في DB::transaction()
- [ ] اختبار أن العملية ترجع تماماً إذا فشلت
- [ ] إضافة معالجة الأخطاء المناسبة

**الوقت المقدر:** 1 ساعة

---

### [ ] P0.7 - إضافة تحديد السرعة (Rate Limiting)
**الملف:** `backend/app/Http/Kernel.php` والمسارات

**المهام:**
- [ ] إضافة تحديد السرعة على POST /login (5 محاولات كحد أقصى لكل 15 دقيقة)
- [ ] إضافة تحديد السرعة على POST /register (3 كحد أقصى في الساعة)
- [ ] إضافة تحديد السرعة العام على API (60 طلب كحد أقصى في الدقيقة)
- [ ] اختبار أن الحدود تعمل

**الكود:**
```php
Route::post('/login', [AuthController::class, 'login'])->middleware('throttle:5,15'); // 5 per 15 min
Route::post('/register', [AuthController::class, 'register'])->middleware('throttle:3,60');
```

**الوقت المقدر:** 1-2 ساعة

---

### [ ] P0.8 - إضافة تنظيف الإدخال (Input Sanitization)
**الملف:** جميع المدخلات

**المهام:**
- [ ] إضافة `htmlspecialchars()` على جميع مدخلات النص
- [ ] استخدام Prepared Statements لجميع الاستعلامات (تم بالفعل بواسطة Eloquent)
- [ ] إضافة التحقق من تحميل الملفات (الحجم، النوع، MIME)
- [ ] الاختبار ببيانات ضارة

**الوقت المقدر:** 2 ساعة

---

## 🟡 P1 - رئيسي (الإصلاح هذا الأسبوع)

### [ ] P1.1 - تنفيذ الترقيم (Pagination) في الواجهة الخلفية
**الملف:** `backend/app/Http/Controllers/Api/AdminController.php`

**المهام:**
- [ ] تعديل getStudents() لدعم `?page=1&limit=10`
- [ ] تعديل getTeachers() للترقيم
- [ ] تعديل getModules() للترقيم
- [ ] إرجاع `{ data: [], total, currentPage, lastPage, perPage }`
- [ ] الاختبار بقيم صفحات مختلفة

**هيكل الكود:**
```php
public function getStudents(Request $request)
{
    $page = $request->get('page', 1);
    $limit = $request->get('limit', 10);
    
    $students = Student::paginate($limit, ['*'], 'page', $page);
    
    return response()->json($students);
}
```

**الوقت المقدر:** 2 ساعة

---

### [ ] P1.2 - تنفيذ الترقيم في الواجهة الأمامية
**الملفات:** 
- `frontend/src/components/admin/students/StudentsList.vue`
- `frontend/src/components/admin/teachers/TeachersList.vue`
- قوائم أخرى

**المهام:**
- [ ] إضافة حالة للصفحة والحد والإجمالي
- [ ] إنشاء مكون PaginationBar قابل لإعادة الاستخدام
- [ ] تنفيذ الأزرار: السابق، التالي، أرقام الصفحات
- [ ] اختبار أن الصفحات تتغير بشكل صحيح

**الوقت المقدر:** 3 ساعات

---

### [ ] P1.3 - تنفيذ البحث والتصفية في الواجهة الخلفية
**الملف:** `backend/app/Http/Controllers/Api/AdminController.php`

**المهام:**
- [ ] تعديل getStudents() لقبول `?search=name&specialty=123&status=active`
- [ ] تعديل getTeachers() للبحث عن الاسم/البريد الإلكتروني
- [ ] تعديل getModules() للبحث
- [ ] استخدام `->where('name', 'like', "%$search%")`
- [ ] اختبار عمليات البحث

**هيكل الكود:**
```php
public function getStudents(Request $request)
{
    $query = Student::query();
    
    if ($request->search) {
        $query->where('first_name', 'like', "%{$request->search}%")
              ->orWhere('last_name', 'like', "%{$request->search}%")
              ->orWhere('registration_number', 'like', "%{$request->search}%");
    }
    
    if ($request->specialty) {
        $query->where('specialty_id', $request->specialty);
    }
    
    return response()->json($query->paginate());
}
```

**الوقت المقدر:** 2 ساعة

---

### [ ] P1.4 - تنفيذ البحث والتصفية في الواجهة الأمامية
**الملفات:** 
- `frontend/src/components/admin/students/StudentFilters.vue`
- `frontend/src/components/admin/students/StudentsList.vue`

**المهام:**
- [ ] إضافة حقل البحث
- [ ] إضافة القائمة المنسدلة للتصفية حسب التخصص/الحالة
- [ ] استدعاء getStudents() مع المرشحات
- [ ] تأخير البحث (Debounce 500ms)
- [ ] اختبار المرشحات

**الوقت المقدر:** 3 ساعات

---

### [ ] P1.5 - تنفيذ لوحة المعلومات للطالب (StudentController::dashboard())
**الملف:** `backend/app/Http/Controllers/Api/StudentController.php`

**المهام:**
- [ ] تنفيذ dashboard() لإرجاع:
  - الدروس القادمة (الـ 5 الأولى)
  - آخر الدرجات (آخر 3 درجات)
  - الحضور (نسبة الحضور هذا الشهر)
  - الواجبات المعلقة (الواجبات المعلقة)
  - عدد الرسائل غير المقروءة
- [ ] حساب الإحصائيات
- [ ] اختبار أن البيانات كاملة

**الوقت المقدر:** 2 ساعة

---

### [ ] P1.6 - إكمال النماذج - إضافة العلاقات
**الملف:** `backend/app/Models/`

**المهام:**
- [ ] إضافة العلاقات في Grade.php:
  - `belongsTo(Student)`, `belongsTo(Module)`, `belongsTo(Exam)`
- [ ] إضافة العلاقات في Deliberation.php:
  - `belongsTo(Student)`, `belongsTo(TrainingSession)`
- [ ] إضافة العلاقات في Attendance.php:
  - `belongsTo(Student)`, `belongsTo(Lesson)`
- [ ] إضافة العلاقات في Homework.php:
  - `belongsTo(Teacher)`, `belongsTo(Module)`, `hasMany(HomeworkSubmission)`
- [ ] إضافة العلاقات في Message.php:
  - `belongsTo(User, 'sender_id')`, `belongsTo(User, 'recipient_id')`
- [ ] الاختبار مع وحدة التحكم: `php artisan tinker`

**الوقت المقدر:** 1 ساعة

---

### [ ] P1.7 - إضافة التحقق من صحة النماذج في الواجهة الأمامية
**الملف:** جميع `.vue` بها نماذج

**المهام:**
- [ ] تثبيت `vee-validate` و `yup`:
  ```bash
  npm install vee-validate yup
  ```
- [ ] إنشاء مكون FormField قابل لإعادة الاستخدام مع الأخطاء
- [ ] إضافة التحقق لكل نموذج:
  - البريد الإلكتروني (صيغة صحيحة، مطلوب)
  - الهاتف (صيغة صحيحة: 10 أرقام)
  - كلمة المرور (حد أدنى 6 أحرف)
  - التاريخ (صيغة صحيحة)
  - الحقول المطلوبة
- [ ] عرض رسائل خطأ واضحة
- [ ] تعطيل الإرسال إذا كانت البيانات غير صحيحة
- [ ] اختبار ببيانات غير صحيحة

**الوقت المقدر:** 4 ساعات

---

### [ ] P1.8 - إكمال المكونات المفقودة
**الملف:** `frontend/src/components/` و `frontend/src/views/`

**المهام:**
- [ ] التحقق من اكتمال `MessagesList.vue`
- [ ] التحقق من اكتمال `DeliberationsList.vue`
- [ ] التحقق من اكتمال `HomeworkSubmissionForm.vue`
- [ ] التحقق من اكتمال `AttendanceMarker.vue`
- [ ] التحقق من اكتمال `GradeEntry.vue`
- [ ] تنفيذ المكونات المفقودة

**الوقت المقدر:** 4-5 ساعات

---

## 🟢 P2 - ثانوي (الإصلاح الأسابيع القادمة)

### [ ] P2.1 - إضافة مكون الإشعارات العام
**الملف:** `frontend/src/components/common/ToastNotification.vue` (إنشاء)

**المهام:**
- [ ] إنشاء مكون Toast مع الموضع والنوع (نجاح/خطأ/تحذير/معلومة) والرسالة
- [ ] إنشاء مكون ToastContainer لعرض الإشعارات
- [ ] إنشاء متجر لإدارة الإشعارات
- [ ] إضافة ToastContainer في App.vue
- [ ] اختبار الإشعارات

**الوقت المقدر:** 2 ساعة

---

### [ ] P2.2 - إضافة حالات التحميل العام
**الملف:** `frontend/src/components/common/GlobalLoader.vue` (إنشاء)

**المهام:**
- [ ] إنشاء مكون محمل عام
- [ ] إنشاء متجر لتتبع الطلبات الجارية
- [ ] إضافة مقاطع اعتراض Axios لتشغيل المحمل
- [ ] اختبار أن المحمل يظهر أثناء الطلبات

**الوقت المقدر:** 1-2 ساعة

---

### [ ] P2.3 - إضافة محملات الهيكل العظمي (Skeleton Loaders)
**الملف:** `frontend/src/components/common/SkeletonLoader.vue` (إنشاء)

**المهام:**
- [ ] إنشاء مكون محمل هيكل عام
- [ ] إضافة في StudentsList أثناء التحميل
- [ ] إضافة في TeachersList أثناء التحميل
- [ ] إضافة في القوائم الأخرى

**الوقت المقدر:** 2 ساعة

---

### [ ] P2.4 - حفظ تفضيل الوضع الداكن
**الملف:** `frontend/src/stores/theme.js`

**المهام:**
- [ ] حفظ اختيار الداكن/الفاتح في localStorage
- [ ] تحميل التفضيل عند بدء التطبيق
- [ ] اختبار أن يبقى عند التحديث

**الوقت المقدر:** 30 دقيقة

---

### [ ] P2.5 - توحيد المناطق الزمنية (Timezones)
**الملف:** جميع الملفات التي تستخدم التواريخ

**المهام:**
- [ ] تثبيت `dayjs` أو `date-fns`
- [ ] تحويل جميع الطوابع الزمنية إلى UTC في الواجهة الخلفية
- [ ] التحويل إلى المنطقة الزمنية المحلية في الواجهة الأمامية عند العرض
- [ ] اختبار التواريخ مع مناطق زمنية مختلفة

**الوقت المقدر:** 2 ساعة

---

### [ ] P2.6 - إضافة فهارس قاعدة البيانات
**الملف:** `backend/database/migrations/`

**المهام:**
- [ ] إنشاء ملف هجرة: `php artisan make:migration add_indexes`
- [ ] إضافة فهارس على:
  - users.email
  - students.registration_number
  - students.specialty_id
  - teachers.specialization
  - modules.specialty_id
  - grades.student_id
  - attendance.student_id
- [ ] تنفيذ الهجرة: `php artisan migrate`
- [ ] التحقق مع `SHOW INDEXES FROM table_name;`

**الوقت المقدر:** 1 ساعة

---

### [ ] P2.7 - إضافة الحذف الناعم (Soft Deletes)
**الملف:** 
- `backend/app/Models/User.php`
- `backend/app/Models/Student.php`
- نماذج أخرى

**المهام:**
- [ ] إنشاء ملف هجرة: `php artisan make:migration add_soft_deletes`
- [ ] إضافة عمود deleted_at للجداول: users, students, teachers
- [ ] إضافة `use SoftDeletes;` للنماذج
- [ ] اختبار أن الحذف ناعم (البيانات لا تزال في قاعدة البيانات)

**الوقت المقدر:** 1 ساعة

---

### [ ] P2.8 - إضافة توثيق API (Swagger)
**الملف:** `backend/`

**المهام:**
- [ ] تثبيت `l5-swagger`:
  ```bash
  composer require "darkaonline/l5-swagger"
  ```
- [ ] توليد الإعدادات: `php artisan vendor:publish --provider "L5Swagger\L5SwaggerServiceProvider"`
- [ ] إضافة تعليقات OpenAPI على المتحكمات
- [ ] توليد المستندات: `php artisan l5-swagger:generate`
- [ ] الوصول إلى http://localhost:8000/api/documentation

**الوقت المقدر:** 3 ساعات

---

### [ ] P2.9 - إضافة اختبارات الوحدة
**الملف:** `backend/tests/`

**المهام:**
- [ ] إنشاء اختبار AuthController::login()
- [ ] إنشاء اختبار AuthController::register()
- [ ] إنشاء اختبار AdminController::createStudent()
- [ ] تشغيل الاختبارات: `php artisan test`
- [ ] الهدف: على الأقل 80٪ من تغطية الكود

**الوقت المقدر:** 4 ساعات

---

### [ ] P2.10 - إضافة التسجيل (Logging)
**الملف:** `backend/config/logging.php` والمتحكمات

**المهام:**
- [ ] تكوين Monolog (تم بالفعل بواسطة Laravel)
- [ ] إضافة `Log::info()`, `Log::error()` في المتحكمات الحرجة
- [ ] إضافة التسجيل لـ:
  - تسجيل دخول المستخدم
  - تعديل البيانات
  - الأخطاء
- [ ] التحقق من السجلات في `storage/logs/`

**الوقت المقدر:** 1-2 ساعة

---

## 📊 الإحصائيات

| الفئة | العدد | الوقت |
|-------|-------|-------|
| P0 - حرجة | 8 | 12-16 ساعة |
| P1 - رئيسي | 8 | 20-25 ساعة |
| P2 - ثانوي | 10 | 18-21 ساعة |
| **المجموع** | **26** | **50-62 ساعة** |

---

## 🎯 الترتيب الموصى به للعمل

**اليوم 1-2 (P0 - حرجة):**
1. P0.1 - إصلاح نموذج User
2. P0.2 - التحقق من الإدخال
3. P0.3 - محاذاة المسارات
4. P0.4 - Auth store الأدوار
5. P0.5 - Axios interceptors
6. P0.6 - معاملات قاعدة البيانات
7. P0.7 - تحديد السرعة
8. P0.8 - تنظيف الإدخال

**اليوم 3-5 (P1 - رئيسي):**
1. P1.1 & P1.2 - الترقيم
2. P1.3 & P1.4 - البحث/التصفية
3. P1.5 - لوحة معلومات StudentController
4. P1.6 - العلاقات في النماذج
5. P1.7 - التحقق من صحة نماذج الواجهة الأمامية
6. P1.8 - المكونات المفقودة

**اليوم 6-7 (P2 - ثانوي):**
- اختيار P2 الأكثر أهمية والعمل عليها

---

## ✅ قائمة التحقق النهائية

قبل النشر:
- [ ] تم إصلاح جميع P0
- [ ] تم إصلاح جميع P1
- [ ] اختبارات الوحدة تنجح
- [ ] لا توجد console.error() في السجلات
- [ ] لا توجد تعليقات TODO في الإنتاج
- [ ] التوثيق محدث
- [ ] سجل التغييرات محدث
- [ ] اكتملت مراجعة الكود

---

**آخر تحديث:** 2026-06-13
